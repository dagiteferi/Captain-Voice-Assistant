[project]
name = "captain-voice-assistant"
version = "0.1.0"
description = "Captain Voice Assistant backend"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
